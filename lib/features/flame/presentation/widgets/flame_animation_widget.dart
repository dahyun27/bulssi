import 'package:flutter/material.dart';

class FlameAnimationWidget extends StatelessWidget {
  const FlameAnimationWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('🔥', style: TextStyle(fontSize: 96));
  }
}
